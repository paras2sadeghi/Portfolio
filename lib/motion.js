// Shared premium easing curves and timing for the whole site.

// expo-out — the signature editorial deceleration
export const EASE = [0.16, 1, 0.3, 1];

// slightly softer variant for larger blocks
export const EASE_SOFT = [0.22, 1, 0.36, 1];

export const DURATION = {
  fast: 0.6,
  base: 1,
  slow: 1.4,
};
